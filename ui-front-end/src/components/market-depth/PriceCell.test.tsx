import { render, screen } from '@testing-library/react'

// exmaples of react unit test:
// test("List renders successfully", () => {
//     render(<TestWithMockData data={mockData} />)
//     expect(screen.getByText(/fletcher/i)).toBeInTheDocument();
// })

// describe('Testing sum', () => {
//     function sum(a, b) {
//        return a + b;
//     }

//     it('should equal 4',()=>{
//        expect(sum(2,2)).toBe(4);
//       })

//     test('also should equal 4', () => {
//         expect(sum(2,2)).toBe(4);
//       }) 
// });


// Price Cell, test point: 
    // price increase effects
    // prince decrease effects
    // no change effects   
    // lastprevious price & current price are reindered

// Call with Steve;
    // good idea to test but the testing suite used for this project is vtest - steve to send some more info on this
